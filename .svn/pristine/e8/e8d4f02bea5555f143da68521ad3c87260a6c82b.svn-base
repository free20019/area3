package com.tw.test;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Random;

public class Test15 {
	
	public static void main(String[] args) {
		/*
		int [] a=new int[5];
		for(int i=0;i<5;i++){
		      int tmp=(int)(Math.random()*15)+1;
		      int index=i;
	    	  while(index>0&&tmp==a[index-1]){
	    		  tmp=(int)(Math.random()*15)+1;
	    		  index--;
	    		  if(index==0){
	    			  index=1;
	    		  }
		      }
		      a[i] = tmp;
	     }
		for(int i=0;i<a.length;i++){
		      System.out.print(a[i]+"   ");
		}
		System.out.println("  ");
		 Arrays.sort(a);  //进行排序
	    for(int i: a){
		         System.out.print(i+"  ");
		}
	    
	    
	    int ac1 = a[1]-a[0];
	    int ac2 = a[2]-a[0];
	    int ac3 = a[3]-a[0];
	    int ac4 = a[4]-a[0];
	    int ac5 = a[2]-a[1];
	    int ac6 = a[3]-a[1];
	    int ac7 = a[4]-a[1];
	    int ac8 = a[3]-a[2];
	    int ac9 = a[4]-a[2];
	    int ac10 = a[4]-a[3];
	    int[] str = {ac1,ac2,ac3,ac4,ac5,ac6,ac7,ac8,ac9,ac10};
	    List<Integer> list = new ArrayList<Integer>();
	    for (int i=0; i<str.length; i++) {
	    	if(!list.contains(str[i])) {//如果数组 list 不包含当前项，则增加该项到数组中
	    		list.add(str[i]);
	    }
	    }	
	    	System.out.println();  
	    	for(int i=0;i<list.size();i++){
	    		System.out.print(list.get(i)+"  ");
	    	}
	    	
		int[] a = new int[5];
		Random random = new Random ();  
        boolean[]  bool = new boolean[14];  
        int randInt = 0;  
        for(int j = 0; j < 5 ; j++) {  
            do{  
                randInt  = random.nextInt(14);  
            }while(bool[randInt]);   
            bool[randInt] = true;  
            a[j]=randInt+1;
        }
        Arrays.sort(a);  //进行排序
        System.out.println();  
	    for(int i: a){
		         System.out.print(i+"  ");
		}
        int ac1 = a[1]-a[0];
	    int ac2 = a[2]-a[0];
	    int ac3 = a[3]-a[0];
	    int ac4 = a[4]-a[0];
	    int ac5 = a[2]-a[1];
	    int ac6 = a[3]-a[1];
	    int ac7 = a[4]-a[1];
	    int ac8 = a[3]-a[2];
	    int ac9 = a[4]-a[2];
	    int ac10 = a[4]-a[3];
	    int[] str = {ac1,ac2,ac3,ac4,ac5,ac6,ac7,ac8,ac9,ac10};
	    List<Integer> list = new ArrayList<Integer>();
	    for (int i=0; i<str.length; i++) {
	    	if(!list.contains(str[i])) {//如果数组 list 不包含当前项，则增加该项到数组中
	    		list.add(str[i]);
	    }
	    }	
	    	System.out.println();  
	    	for(int i=0;i<list.size();i++){
	    		System.out.print(list.get(i)+"  ");
	    	}
        System.out.println();
        System.out.println(list.size());
        */
        
		String s="2014-05-05";
		String e = s.substring(2, 4)+s.substring(5, 7);
		System.out.println(e);
		
	}
}
